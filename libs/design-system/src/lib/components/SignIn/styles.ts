const container = {
  marginTop: '6rem',
}

const paper = {
  position: 'relative',
  backgroundColor: 'paper.background.secondary',
}

const content = {
  padding: '3rem',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
}

const icon = {
  position: 'absolute',
  top: '-50px',
  left: 0,
  right: 0,
  marginLeft: 'auto',
  marginRight: 'auto',
  width: '100px',
}

const formContainer = { mt: 1 };

const submitButton = { mt: 3, mb: 2 };

const copyright = { mt: 4, mb: 2 };

const signUpLink = { cursor: 'pointer' };

export default {
  container,
  paper,
  content,
  formContainer,
  icon,
  submitButton,
  copyright,
  signUpLink
}